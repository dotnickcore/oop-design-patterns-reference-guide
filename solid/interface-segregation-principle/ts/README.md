# TypeScript
The application of Interface Segregation in TypeScript is achieved by interfaces split into smaller modules.
A checklist to determine if Interface Segregation is needed:
1. Do I have a big interface doing everything for me?
2. Can each function be split into something smaller?